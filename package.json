{
  "name": "homework-helper-parent",
  "version": "0.41.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "start": "npx --yes serve dist -l tcp://0.0.0.0:$PORT",
    "dev": "npx --yes serve . -l 3000"
  },
  "engines": {
    "node": ">=20"
  }
}
